Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  Your application can be accessed at: https://{{ $host.host }}
{{- end }}
{{- end }}

{{- if .Values.database.external.enabled }}
Database connection configured. To test connectivity:

  $ kubectl run db-test --rm -it --image=postgres:15-alpine --restart=Never \
    --namespace {{ .Release.Namespace }} \
    -- psql -h {{ .Values.database.external.host }} -U {{ .Values.database.external.username }} -d {{ .Values.database.external.database }}
{{- end }}

{{- if .Values.oidc.enabled }}
OIDC is enabled. Configure your OIDC provider with:
  - Issuer: {{ .Values.oidc.issuer }}
  - Client ID: {{ .Values.oidc.clientId }}
  - Redirect URI: {{ .Values.oidc.redirectUri }}
{{- end }}

For more information, visit: https://github.com/joryirving/KubeTix
